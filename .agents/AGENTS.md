# Mediflow BPI System Agent Rules

- **Always Push Changes to GitHub**: Whenever any source files are added, modified, or deleted, automatically stage the changes (`git add .`), commit them with a descriptive message, and push them to the remote GitHub repository (`git push`).
